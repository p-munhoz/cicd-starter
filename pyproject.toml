[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "cicd-starter"
version = "0.1.0"
description = "Companion package for the CI/CD in Practice series"
requires-python = ">=3.10"
